{
    "retries": {
        "max_retries": 30,
        "retry_delay": 2
    },
    "kafka": {
        "consumer": {
            "session_timeout_ms": 30000,
            "heartbeat_interval_ms": 10000,
            "max_poll_interval_ms": 300000,
            "max_poll_records": 100
        }
    },
    "email": {
        "default_recipients": ["alerts@example.com"],
        "operations_team": ["operations@example.com"]
    }
}